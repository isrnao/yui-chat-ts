{
  "name": "yui-chat-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "lint": "pnpm --filter @yui/mobile run lint",
    "test": "pnpm --filter @yui/mobile run test",
    "start": "pnpm --filter @yui/mobile run start",
    "ios": "pnpm --filter @yui/mobile run ios",
    "android": "pnpm --filter @yui/mobile run android"
  }
}
